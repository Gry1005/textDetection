import numpy as np
import cv2
import random

#localheight_img = np.zeros((2, 2, 3), np.uint8)

#cv2.line(localheight_img, (0,0), (1,1), (3.6, 0, 0), 1)

#print(localheight_img)

for i in range(0,3):
    print(i)